// const path = require('path')
// const os = require('os')
const Conf = require('conf')

// const configFilePath = path.join(os.tmpdir(), 'report_telegram_config')

const config = new Conf();

module.exports = {
  config
}